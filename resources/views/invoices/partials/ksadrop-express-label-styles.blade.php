    <style>
        @page { size: 4in 6in; margin: 3mm; }
        * { box-sizing: border-box; }
        body { font-family: 'DejaVu Sans', sans-serif; font-size: 9px; color: #000; margin: 0; padding: 0; }
        table { width: 100%; border-collapse: collapse; table-layout: fixed; }
        td { vertical-align: top; }
        .grid td { border: 1px solid #000; }
        .grid .plain td { border: none; padding: 0; }
        .pad { padding: 4px 6px; }
        .k { font-size: 7px; font-weight: bold; text-transform: uppercase; letter-spacing: 0.4px; }
        .b { font-weight: bold; }
        .up { text-transform: uppercase; }
        .center { text-align: center; }
        .right { text-align: right; }
        .fit { line-height: 1.3; overflow: hidden; padding-right: 4px; word-wrap: break-word; overflow-wrap: break-word; }

        .brand { font-size: 19px; font-weight: bold; letter-spacing: 1.5px; }
        .brand-sub { font-size: 6.5px; letter-spacing: 1px; }
        .svc { font-size: 11px; font-weight: bold; }
        .badge { background: #000; color: #fff; font-size: 16px; font-weight: bold; text-align: center; vertical-align: middle; }

        .barcode img { width: 94%; height: 56px; }
        .awb { font-size: 17px; font-weight: bold; letter-spacing: 2.5px; margin-top: 2px; }

        .sign-line { border-bottom: 1px solid #000; height: 14px; }
        .order-barcode img { width: 100%; height: 22px; }
        .qr img { width: 64px; height: 64px; }
    </style>
